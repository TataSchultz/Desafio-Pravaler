/* eslint-disable react/prop-types */
import './botao.css'


export const Botao = (props) => (
    
    <div className="botao">
        
        <button type={props.type} placeholder={props.placeholder} > </button>

    </div>
);
